[pytest]
testpaths = .
python_files = test_*.py
markers =
    smoke: Quick health and critical path for this BC
    regress: Full functional coverage for this BC (stack must be up)
addopts = -ra --strict-markers
